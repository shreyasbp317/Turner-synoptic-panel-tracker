DATABASE_URL="postgresql://postgres:password@127.0.0.1:54329/rpl10x"
SESSION_SECRET="replace-with-a-long-random-secret-at-least-32-chars"
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="replace-me"
ADMIN_NAME="Admin"
UPLOAD_DIR="./uploads"

# --- Railway production ---
# DATABASE_URL=${{Postgres.DATABASE_URL}}   # set as a Railway variable reference
# UPLOAD_DIR=/app/uploads                   # must match the Volume mount path
# NODE_ENV=production
